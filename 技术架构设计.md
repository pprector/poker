# PokerGo 技术架构设计

> **文档版本**：V2.1（架构调整：统一管理后台 Vben 5 + 多租户方案）  
> **编写日期**：2026年6月9日  
> **文档状态**：评审稿  
> **关联文档**：[MVP产品需求文档](./MVP产品需求文档.md) | [产品调研文档](./产品调研文档.md)

---

## 目录

1. [技术选型总览](#1-技术选型总览)
2. [系统架构](#2-系统架构)
3. [前端架构](#3-前端架构)
4. [后端架构](#4-后端架构)
5. [API 设计规范](#5-api-设计规范)
6. [数据库设计](#6-数据库设计)
7. [认证与安全](#7-认证与安全)
8. [部署架构](#8-部署架构)
9. [项目结构](#9-项目结构)
10. [后端脚手架选型](#10-后端脚手架选型)

---

## 1. 技术选型总览

| 层级 | 技术 | 版本 | 选型理由 |
|------|------|------|----------|
| **玩家端** | uni-app (Vue 3) + uv-ui | HBuilderX 4.x / Vue 3.5 | 团队技术栈统一（Vue 3）、组件库丰富、白标小程序友好、多端备用能力 |
| **统一管理后台** | Vue 3 + Ant Design Vue（Vben 5） | Vue 3.5+ / Ant Design Vue 4.x | 基于芋道 yudao-ui-admin-vben（Vben 5.x 架构），一套后台覆盖经营者和系统管理员两种角色，通过权限控制菜单展示 |
| **玩家端** | uni-app (Vue 3) + uv-ui | HBuilderX 4.x / Vue 3.5 | 团队技术栈统一（Vue 3）、组件库丰富、白标小程序友好、多端备用能力 |
| **后端脚手架** | 芋道 ruoyi-vue-pro | v2026.05 | 内置微信登录/COS/短信/RBAC，Spring Boot 3.4 + Sa-Token，MVP 效率最大化 |
| **后端框架** | Spring Boot | 3.4.x / Java 21 | 最新 LTS，虚拟线程 GA、Record Patterns、GraalVM 原生编译就绪 |
| **ORM** | MyBatis-Plus | 3.5.9+ | Lambda 链式查询、分页插件、乐观锁、代码生成器 |
| **数据库** | MySQL | 8.0+ | 关系型数据首选，JSON 字段支持，窗口函数便于排行榜查询 |
| **缓存** | Redis | 7.x | 验证码存储、会话管理、热点数据缓存、排行榜 Sorted Set |
| **API 文档** | Knife4j | 4.5+ | OpenAPI 3.0 自动文档，在线调试，一键导出 |
| **认证** | Sa-Token + 微信 OAuth | 1.39+ | 注解式鉴权，比 Spring Security 配置量少 70%，内置多端同时登录管理 |
| **文件存储** | 腾讯云 COS / 阿里云 OSS | — | 门店照片、知识库图片、商品图片（芋道内置 OSS 模块统一管理） |
| **构建工具** | Vite (前端) + Maven (后端) | Vite 6.x | Vite 开发体验极佳；Maven 是 Java 标准构建工具 |
| **部署** | Docker + Nginx | — | 容器化部署，Nginx 反向代理 + 静态资源 |
| **UI 设计规范** | 暗色主题 + 渐变卡片 + 毛玻璃 | — | 匹配德扑/酒馆场景的高端深色风格，兼顾可读性与氛围感 |

---

## 2. 系统架构

### 2.1 整体架构图

```
┌──────────────────────────────────────────────────────────────────────────────────┐
│                                  客户端层                                         │
│                                                                                   │
│  ┌─────────────────────────────────────────────────┐  ┌───────────────────────┐  │
│  │           统一管理后台（Vben 5 + Ant Design Vue） │  │  微信小程序（玩家端）    │  │
│  │                                                 │  │                       │  │
│  │  ┌───────────────────┐  ┌───────────────────┐  │  │  uni-app (Vue 3)      │  │
│  │  │ 系统管理员视角     │  │ 门店经营者视角     │  │  │  uv-ui / 多端就绪     │  │
│  │  │ 租户管理/知识库   │  │ 工作台/活动/订单  │  │  │                       │  │
│  │  │ 合规文档/系统配置 │  │ 商品/会员/经营设置 │  │  │                       │  │
│  │  │ 数据字典/日志监控 │  │ 品牌主题/门店编辑 │  │  │                       │  │
│  │  └────────┬──────────┘  └────────┬──────────┘  │  └──────────┬────────────┘  │
│  │           │                      │              │             │                 │
│  │   同一套代码，同⼀个部署单元，通过 RBAC 权限控制菜单展示   │     │              │
│  └─────────────┬──────────────────────┬────────────┘            │                 │
│                │                      │                         │                 │
│                │        HTTPS / WSS   │                         │                 │
└────────────────┼──────────────────────┼─────────────────────────┼─────────────────┘
                 │                      │                         │
┌────────────────┼──────────────────────┼─────────────────────────┼─────────────────┐
│                │               接入层  │                         │                 │
│  ┌─────────────┴──────────────────────┴─────────────────────────┴────────────────┐  │
│  │                          Nginx (反向代理)                                       │  │
│  │  • /api/*          → 转发至 Spring Boot :8080                                  │  │
│  │  • /admin/*        → 静态资源 /opt/poker-go-system/  (统一管理后台 SPA)          │  │
│  │  • /public/*       → 静态资源 /opt/static/ (图片等)                            │  │
│  └────────────────────────────────────┬──────────────────────────────────────────┘  │
│                                       │                                             │
├─────────────────────────────────────┼───────────────────────────────────────────────┤
│                                     │              应用层                            │
│  ┌──────────────────────────────────┴────────────────────────────────────────────┐  │
│  │                    Spring Boot 3.4 (Java 21)  :8080                            │  │
│  │                                                                                │  │
│  │  ┌─────────── 业务模块（自建）──────────────────────────────────────────────┐  │  │
│  │  │ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐     │  │  │
│  │  │ │ 门店   │ │ 活动   │ │ 订单   │ │ 会员   │ │ 知识库  │ │ 排行榜  │     │  │  │
│  │  │ ├────────┤ ├────────┤ ├────────┤ ├────────┤ ├────────┤ ├────────┤     │  │  │
│  │  │ │ 商品   │ │ 合规   │ │ 通知   │ │ 认证   │ │(空留)  │ │(空留)  │     │  │  │
│  │  │ └────────┘ └────────┘ └────────┘ └────────┘ └────────┘ └────────┘     │  │  │
│  │  └────────────────────────────────────────────────────────────────────────┘  │  │
│  │                                                                                │  │
│  │  ┌─────────── 系统模块（脚手架内置）────────────────────────────────────────┐  │  │
│  │  │ 用户管理 | 角色权限 | 菜单管理 | 字典管理 | 操作日志 | 代码生成           │  │  │
│  │  │ 社交登录 | 文件存储(OSS) | 短信服务 | API日志 | 定时任务                  │  │  │
│  │  └────────────────────────────────────────────────────────────────────────┘  │  │
│  │                                                                                │  │
│  │  ┌─────────── 公共组件 ────────────────────────────────────────────────────┐  │  │
│  │  │ 认证(Sa-Token) | 权限(RBAC) | 异常处理 | 日志(AOP)                       │  │  │
│  │  │ 参数校验(Validator) | API文档(Knife4j) | 对象转换(MapStruct)              │  │  │
│  │  └────────────────────────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────┬────────────────────────────────────────────┘  │
│                                     │                                               │
├─────────────────────────────────────┼───────────────────────────────────────────────┤
│                                     │              数据层                            │
│  ┌──────────────────┐  ┌───────────┴──────────┐  ┌────────────────────────────┐   │
│  │    MySQL 8.0     │  │       Redis 7        │  │       腾讯云 COS           │   │
│  │   (业务+系统数据)  │  │  (登录态/缓存/排行榜)  │  │    (图片/文件存储)          │   │
│  └──────────────────┘  └──────────────────────┘  └────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────────────────────────┘
```

> **两个客户端的关系**：
> - **微信小程序**（玩家端）：面向玩家，用于浏览门店、报名活动、查看排行榜、下单酒水
> - **统一管理后台**（Vben 5 + Ant Design Vue）：基于芋道官方 yudao-ui-admin-vben 项目（Vben 5.x 架构），**一套代码覆盖经营者和系统管理员两种角色**。通过 RBAC 权限控制菜单展示，经营者看到门店运营菜单（工作台/活动/订单/商品/会员），系统管理员看到平台管理菜单（租户管理/知识库/合规/系统配置）。后端 API 服务统一为所有前端提供 RESTful API。
> - **后端 API 服务**（Spring Boot）：统一为小程序和统一管理后台提供 RESTful API，通过 Sa-Token 注解鉴权 + tenant_id 多租户隔离区分不同角色的访问权限

### 2.2 架构原则

| 原则 | 说明 |
|------|------|
| **前后端分离** | 小程序 / Vue 管理后台通过 HTTPS 调用 RESTful API，后端不渲染页面 |
| **无状态服务** | 后端不存储会话状态，认证通过 Sa-Token（Token 存储于 Redis）传递，便于水平扩展 |
| **手机优先** | API 响应体积精简，管理后台 UI 优先适配 375-414px 竖屏 |
| **分层解耦** | Controller → Service → Mapper 三层，Service 层承载全部业务逻辑 |
| **读写分离准备** | 数据库设计预留读写分离扩展性（但 MVP 不实施） |

---

## 3. 前端架构

### 3.1 微信小程序（玩家端）

#### 技术栈

| 项 | 选型 | 说明 |
|-----|------|------|
| 框架 | uni-app (Vue 3 + `<script setup>`) | 与管理后台同为 Vue 3 语法，团队技术栈统一，白标复制成本极低 |
| UI 组件库 | uv-ui（或 uni-ui） | 60+ 移动端组件，适配微信/支付宝/H5，开发效率比原生高 40% |
| 状态管理 | Pinia | 与管理后台统一方案，跨页面状态共享清晰 |
| 网络请求 | 封装 `uni.request` | 统一处理 Token 注入、错误提示、加载状态（与管理后台 Axios 拦截器逻辑对齐） |
| 开发工具 | HBuilderX 4.x + 微信开发者工具 | HBuilderX 编写代码，微信开发者工具预览调试 |
| 编译目标 | 微信小程序（主）| MVP 只编译微信小程序，未来可一键编译支付宝/H5/App |
| 主题方案 | CSS 变量 + 动态注入 | 每个门店可自定义主题色/风格，进入门店时从后端拉取主题配置动态应用 |

#### 多主题设计方案

小程序面向所有酒馆的玩家，不同门店需要展现**不同的品牌调性**。技术实现采用 **CSS Variables 动态注入**方案：

**数据来源**：经营者在管理后台"品牌定制"中设置主题，存储在 `club` 表的 `theme_config` 字段（JSON）：

```json
{
  "primaryColor": "#667eea",
  "secondaryColor": "#764ba2",
  "bgMode": "dark",
  "cardStyle": "glass",
  "fontStyle": "modern",
  "navStyle": "custom"
}
```

**预设主题模板**（经营者可选，降低配置门槛）：

| 主题名 | 主色 | 背景 | 风格 | 适合场景 |
|--------|------|------|------|---------|
| 🌙 暗夜紫金 | `#667eea → #764ba2` | 深色 `#0f0f23` | 毛玻璃卡片 | 高端酒馆、竞技氛围 |
| 🍺 精酿暖棕 | `#f5a623 → #e07c00` | 深棕 `#1a1208` | 木质纹理卡片 | 精酿酒吧、温馨风 |
| 🎲 霓虹赛博 | `#00d4aa → #00b4d8` | 纯黑 `#0a0a0a` | 霓虹边框 | 潮流夜店、电竞风 |
| ☕ 简约轻商务 | `#2563eb → #1d4ed8` | 浅灰 `#f8fafc` | 白底卡片 | 商务俱乐部、简洁风 |
| 🔥 中国红 | `#dc2626 → #991b1b` | 深黑 `#0c0c0c` | 金边装饰 | 传统牌馆、节庆感 |

**技术实现流程**：

```
玩家扫码/搜索进入某门店
  │
  ▼
小程序获取 clubId → 调用 GET /api/v1/clubs/{clubId}/theme
  │
  ▼
后端返回该门店的 theme_config JSON
  │
  ▼
前端将主题变量注入到页面根节点的 CSS Variables
  │
  ▼
所有组件通过 var(--primary-color) 等变量自动适配
```

**代码实现**（`stores/club.js` 中的主题注入逻辑）：

```javascript
// stores/club.js
export const useClubStore = defineStore('club', () => {
  const themeConfig = ref(null)

  async function loadTheme(clubId) {
    const res = await getClubTheme(clubId)
    themeConfig.value = res.data

    // 动态注入 CSS 变量到页面
    const root = document.documentElement || uni.createSelectorQuery()
    const vars = {
      '--primary-color': res.data.primaryColor,
      '--secondary-color': res.data.secondaryColor,
      '--bg-color': res.data.bgMode === 'dark' ? '#0f0f23' : '#f8fafc',
      '--card-bg': res.data.cardStyle === 'glass'
        ? 'rgba(255,255,255,0.05)'
        : 'rgba(255,255,255,0.95)',
      '--text-color': res.data.bgMode === 'dark' ? '#e2e8f0' : '#1a202c',
    }
    // uni-app 中通过 page-meta 或 style 动态绑定
    return vars
  }

  return { themeConfig, loadTheme }
})
```

**uni.scss 中定义默认主题变量**（作为 fallback）：

```scss
// uni.scss - 全局默认主题变量
:root {
  --primary-color: #667eea;
  --secondary-color: #764ba2;
  --bg-color: #0f0f23;
  --card-bg: rgba(255, 255, 255, 0.05);
  --card-radius: 16px;
  --text-color: #e2e8f0;
  --text-muted: #94a3b8;
}
```

**组件中使用**（所有组件统一引用变量，无需关心具体门店主题）：

```vue
<template>
  <view class="event-card">
    <text class="title">{{ event.name }}</text>
  </view>
</template>

<style scoped>
.event-card {
  background: var(--card-bg);
  border-radius: var(--card-radius);
  backdrop-filter: blur(12px);
  border: 1px solid rgba(255, 255, 255, 0.1);
}
.title {
  color: var(--text-color);
}
</style>
```

**缓存策略**：主题配置通过 `uni.setStorageSync` 本地缓存，避免每次打开都请求后端。只有经营者更新主题后，通过版本号比对触发刷新。

#### 目录结构

```
poker-go-miniapp/
├── App.vue                 # 应用入口
├── main.js                 # 初始化（Pinia 挂载等）
├── manifest.json           # uni-app 配置（AppID/编译目标/权限）
├── pages.json              # 路由配置（页面路径/tabBar/窗口）
├── uni.scss                # 全局 SCSS 变量（主题色/间距/圆角）
│
├── pages/                  # 页面目录（Vue 3 SFC）
│   ├── home/index.vue      # 首页（门店展示 + 活动列表）
│   ├── event/detail.vue    # 活动详情 + 报名
│   ├── menu/               # 菜单/点单
│   │   ├── index.vue       # 商品分类列表
│   │   ├── cart.vue        # 购物车
│   │   └── orders.vue      # 我的订单
│   ├── knowledge/index.vue # 知识库（公开内容）
│   ├── leaderboard/index.vue # 排行榜
│   ├── mine/index.vue      # 我的（个人信息/活动/成绩/订单）
│   ├── onboarding/         # 开店向导（4步）
│   │   ├── step1.vue       # 门店信息
│   │   ├── step2.vue       # 品牌定制
│   │   ├── step3.vue       # 知识领取
│   │   └── step4.vue       # 发布
│   └── setting/index.vue   # 设置
│
├── components/             # 通用组件（Vue 3 SFC）
│   ├── NavBar.vue          # 自定义导航栏
│   ├── EventCard.vue       # 活动卡片
│   ├── ProductCard.vue     # 商品卡片
│   ├── EmptyState.vue      # 空状态提示
│   └── LoadingSkeleton.vue # 骨架屏
│
├── api/                    # 接口层（与管理后台 api/ 逻辑对齐）
│   ├── request.js          # uni.request 封装（Token注入/错误拦截）
│   ├── auth.js             # 微信登录
│   ├── club.js             # 门店接口
│   ├── event.js            # 活动接口
│   ├── product.js          # 商品/订单接口
│   └── knowledge.js        # 知识库接口
│
├── stores/                 # Pinia 状态管理
│   ├── user.js             # 用户信息/登录态
│   └── club.js             # 当前门店上下文
│
├── utils/                  # 工具函数
│   ├── auth.js             # 微信登录逻辑
│   ├── storage.js          # 本地存储封装
│   └── validator.js        # 表单校验
│
├── static/                 # 静态资源
│   └── images/             # 图标/默认图片
│
└── env.js                  # 环境配置（API地址/平台模式/默认clubId）
                            # 白标小程序通过此文件切换配置
```

#### 小程序路由设计

| 路径 | 页面 | TabBar | 说明 |
|------|------|--------|------|
| `pages/home/index` | 首页 | ✅ | 含开店向导入口（仅经营者角色可见） |
| `pages/menu/index` | 菜单 | ✅ | 酒水商品浏览 |
| `pages/knowledge/index` | 知识库 | ✅ | 公开内容 + 获客入口 |
| `pages/leaderboard/index` | 排行榜 | ✅ | 本月排名 |
| `pages/mine/index` | 我的 | ✅ | 个人信息/活动/成绩/订单 |
| `pages/event-detail/index` | 活动详情 | ❌ | 从首页卡片进入 |
| `pages/menu/cart/index` | 购物车 | ❌ | 从菜单进入 |
| `pages/menu/orders/index` | 我的订单 | ❌ | 从我的进入 |
| `pages/onboarding/step*-*/index` | 开店向导 | ❌ | 4个子页面 |

#### TabBar 按角色差异化

小程序 TabBar 需根据用户角色动态展示：普通玩家看到"首页/菜单/知识库/排行榜/我的"，经营者角色额外看到"管理"入口（或在"我的"中展示管理后台跳转）。

但由于微信小程序 TabBar 需要在 app.json 中静态声明，MVP 采用以下方案：

- **统一 5 个 Tab**：首页、菜单、知识库、排行榜、我的
- **经营者功能**：集中在"我的"页面中，通过角色判断展示"门店管理"入口（外跳 Web 管理后台）
- **开店向导**：在首页以卡片形式展示，仅未开店用户可见

### 3.2 统一管理后台（Vben 5 + Ant Design Vue）

**核心设计决策**：经营者管理后台与系统后台**合并为一个前端项目**，基于芋道官方 `yudao-ui-admin-vben`（Vben 5.x 架构）。不同角色（`operator` / `admin`）登录后通过 RBAC 权限控制展示不同的菜单和页面。一套代码、一个部署单元，降低维护成本。

#### 技术栈

| 项 | 选型 | 说明 |
|-----|------|------|
| 基础框架 | Vben 5.x（vue-vben-admin v5.7.0） | 企业级中后台框架，Monorepo + Turborepo 架构 |
| UI 组件库 | Ant Design Vue 4.x | PC 端功能完整，同时具备响应式能力，可适配手机竖屏操作 |
| 构建工具 | Vite 8 | 秒级热更新，极致开发体验 |
| CSS 方案 | Tailwind CSS 4 + CSS Variables | 原子化 + 运行时变量，暗色主题原生支持 |
| 路由 | Vue Router 5（Hash 模式） | 简化 Nginx 部署 |
| 状态管理 | Pinia 3 | Vue 3 官方推荐 |
| HTTP 客户端 | Axios（Vben 内置封装） | 拦截器统一处理 Token/错误/Loading |
| 图表 | ECharts 6 | 数据看板可视化 |
| 富文本 | TinyMCE 7 | 知识库/合规文档内容编辑 |
| 表单验证 | vee-validate 4 + zod 3 | 类型安全的表单校验 |
| 国际化 | vue-i18n 11 | 内置 i18n 能力，预留多语言扩展 |

#### 角色与权限映射

```
统一管理后台
│
├── role=admin（系统管理员 / 平台运营）
│   ├── 租户管理（门店列表、套餐管理、过期管理）
│   ├── 知识库管理（文章 CRUD、分类管理）
│   ├── 合规文档管理（版本管理、发布/下架）
│   ├── 主题模板管理（创建/预览/上架/下架）
│   ├── 系统管理（用户/角色/菜单/字典/配置）
│   ├── 操作日志（API 日志、登录日志）
│   └── 平台数据看板
│
├── role=operator（门店经营者 / 酒馆老板）
│   ├── 工作台（今日概览、快捷操作、待处理事项）
│   ├── 活动管理（列表/创建/编辑/报名管理/排名录入）
│   ├── 订单管理（状态 Tab、确认/完成/取消）
│   ├── 商品管理（CRUD、分类、售罄标记）
│   ├── 会员管理（列表/详情）
│   ├── 门店编辑（信息/照片/营业时间）
│   ├── 品牌主题（主题选择/预览/自定义配色）
│   ├── 经营设置（模式模板/积分规则/储值策略）
│   ├── 知识库（阅读，无编辑权限）
│   ├── 合规中心（阅读，无编辑权限）
│   └── 开店向导（4步流程）
│
└── 以上所有页面共享同一套 API 封装、路由守卫、权限校验逻辑
```

#### 多租户下的权限控制流程

```
用户登录 → 后端签发 Token（含角色信息 + tenant_id）
  │
  ▼
Vben 前端路由守卫 → 解析 Token → 获取用户角色
  │
  ├→ role=admin → 加载平台管理路由表（全部菜单可见，跨租户管理）
  │
  └→ role=operator → 加载门店运营路由表（仅见自己门店的操作）
                          │
                          ▼
                    后端 API 层
              TenantLineInnerInterceptor
              自动在 SQL 追加 WHERE tenant_id=?
              各门店数据天然隔离
```

#### 统一管理后台目录结构（在 Vben 5 架构下新增的 PokerGo 业务模块）

```
poker-go-system/                          # 基于 yudao-ui-admin-vben 的 monorepo
├── apps/
│   └── web-antd/                         # Ant Design Vue 应用（主入口）
│       └── src/
│           └── views/
│               ├── system/               # 【脚手架内置】系统管理页面
│               │   ├── tenant/           #   租户管理（admin 可见）
│               │   ├── user/             #   用户管理
│               │   ├── role/             #   角色管理
│               │   └── dict/             #   字典管理
│               │
│               ├── poker/                # 【PokerGo 业务页面】
│               │   ├── dashboard/        #   工作台（operator 首页）
│               │   ├── event/            #   活动管理
│               │   ├── order/            #   订单管理
│               │   ├── club/             #   门店编辑 + 编辑信息
│               │   ├── product/          #   商品管理
│               │   ├── member/           #   会员管理
│               │   ├── knowledge/        #   知识库（admin 编辑 / operator 阅读）
│               │   ├── compliance/       #   合规中心
│               │   ├── leaderboard/      #   排行榜管理
│               │   ├── theme/            #   品牌主题设置
│               │   ├── settings/         #   经营设置 + 模块开关
│               │   └── onboarding/       #   开店向导（4步流程）
│               │
│               └── infra/               # 【脚手架内置】基础设施
│                   ├── file/             #   文件管理
│                   ├── job/              #   定时任务
│                   └── log/              #   日志管理
│
├── packages/                              # Vben 共享包（不改动）
├── internal/                              # Vben 构建配置（不改动）
└── package.json                           # monorepo 根配置
```

#### 统一管理后台路由设计

| 路径 | 页面 | 角色 | 说明 |
|------|------|------|------|
| `/login` | 登录页 | 所有 | 手机号+验证码 或 账号密码 |
| `/poker/dashboard` | 工作台 | operator | 今日概览 + 快捷入口 |
| `/poker/event/list` | 活动列表 | operator | 创建/编辑/报名管理 |
| `/poker/event/create` | 创建活动 | operator | 含盲注模板选择 |
| `/poker/event/:id` | 活动详情 | operator | 报名列表 + 排名录入 |
| `/poker/club/edit` | 编辑门店 | operator | 信息/照片/主题色 |
| `/poker/product/list` | 商品列表 | operator | 添加/编辑/排序/售罄 |
| `/poker/product/category` | 分类管理 | operator | 自定义分类 |
| `/poker/order/list` | 订单列表 | operator | 按状态筛选 |
| `/poker/order/:id` | 订单详情 | operator | 确认/完成/取消 |
| `/poker/member/list` | 会员列表 | operator | 搜索/详情 |
| `/poker/knowledge/list` | 知识库 | 所有 | admin 可编辑 / operator 只读 |
| `/poker/compliance` | 合规中心 | operator | 红线/清单/模板 |
| `/poker/leaderboard` | 排行榜 | operator | 查看/分享 |
| `/poker/onboarding` | 开店向导 | operator | 4步向导流程 |
| `/poker/settings` | 经营设置 | operator | 模式模板/模块开关/推送配置 |
| `/poker/theme` | 品牌主题 | operator | 主题选择/预览/自定义配色 |
| `/system/tenant` | 租户管理 | admin | 门店列表/套餐管理 |
| `/system/user` | 用户管理 | admin | 平台用户管理 |
| `/system/role` | 角色管理 | admin | RBAC 角色配置 |
| `/system/dict` | 字典管理 | admin | 数据字典维护 |
| `/infra/log` | 操作日志 | admin | API 日志/登录日志 |

---

## 4. 后端架构

### 4.1 技术栈

| 项 | 选型 | 说明 |
|-----|------|------|
| 语言 | Java 21 (LTS) | 虚拟线程 GA、Record Patterns、Pattern Matching、密封类、文本块等现代特性 |
| 框架 | Spring Boot 3.4.x | 最新稳定版，虚拟线程 GA、GraalVM 原生编译、Observability 增强 |
| 脚手架 | ruoyi-vue-pro v2026.05 | 仅使用后端部分，开箱即用的认证/权限/OSS/短信/代码生成能力 |
| ORM | MyBatis-Plus 3.5.9+ | Lambda 链式查询、分页/乐观锁/自动填充插件、代码生成器 |
| 数据库连接池 | HikariCP | Spring Boot 默认内置，性能最优 |
| 缓存 | Spring Cache + Redisson | 注解驱动缓存 + 分布式锁，Redis 做验证码/会话/排行榜存储 |
| 认证 | Sa-Token 1.39+ | 注解式鉴权（`@SaCheckPermission`），多端登录管理，比 Spring Security 配置量减少 70% |
| API 文档 | Knife4j 4.5+ | OpenAPI 3.0 自动文档，在线调试，支持分组（管理端/小程序端） |
| 校验 | Hibernate Validator | 注解式参数校验（`@NotNull`、`@Size`、`@Pattern` 等） |
| 工具库 | Hutool 5.x + MapStruct | Hutool 通用工具 + MapStruct 编译期对象映射（替代手写 BeanUtils） |
| JSON | Jackson | Spring Boot 默认，性能优异，全局配置 Long→String 防精度丢失 |
| 文件上传 | 芋道内置 OSS 模块 | 统一对接腾讯云 COS / 阿里云 OSS / MinIO，后台可视化管理 |
| 消息推送 | 微信模板消息 + WebSocket | 赛事提醒用模板消息，订单实时通知用 WebSocket 长连接 |

### 4.2 分层架构

```
┌──────────────────────────────────────────────┐
│               Controller 层                   │
│  接收请求 → 参数校验 → 调用 Service → 返回结果 │
│  @RestController + @Validated                │
└────────────────────┬─────────────────────────┘
                     │
┌────────────────────┴─────────────────────────┐
│                Service 层                     │
│  业务逻辑编排、事务管理、权限判断、缓存操作     │
│  @Service + @Transactional                   │
└────────────────────┬─────────────────────────┘
                     │
┌────────────────────┴─────────────────────────┐
│                Mapper 层                      │
│  数据访问（MyBatis-Plus BaseMapper）           │
│  @Mapper + 自定义 XML（复杂查询）              │
└────────────────────┬─────────────────────────┘
                     │
┌────────────────────┴─────────────────────────┐
│              数据库 / 缓存                     │
│         MySQL 8.0 + Redis 7                  │
└──────────────────────────────────────────────┘
```

### 4.3 模块划分

基于 ruoyi-vue-pro 脚手架，后端按业务领域拆分模块。脚手架已内置的能力（认证、权限、OSS、短信、代码生成等）无需重复开发，我们只需在其上新建业务模块：

```
yudao-server/                            # 启动模块（ruoyi-vue-pro 主项目）
│
├── yudao-framework/                     # 【脚手架内置】核心框架层
│   ├── yudao-spring-boot-starter-security/   # Sa-Token 认证 + RBAC 鉴权
│   ├── yudao-spring-boot-starter-mybatis/    # MyBatis-Plus 分页/自动填充/多租户
│   ├── yudao-spring-boot-starter-redis/      # Redis 缓存 + Redisson 分布式锁
│   ├── yudao-spring-boot-starter-file/       # OSS 文件上传（COS/OSS/MinIO）
│   ├── yudao-spring-boot-starter-sms/        # 短信验证码（阿里云/腾讯云）
│   ├── yudao-spring-boot-starter-websocket/  # WebSocket 实时通信
│   └── yudao-common/                         # 公共工具类、统一响应、异常处理
│       ├── CommonResult.java                 # 统一响应 {code, message, data}
│       ├── PageResult.java                   # 分页响应
│       ├── GlobalExceptionHandler.java       # 全局异常处理
│       └── ErrorCode.java                    # 错误码枚举
│
├── yudao-module-system/                 # 【脚手架内置】系统管理
│   ├── 用户管理 (user)                   # 登录/注册/密码重置
│   ├── 角色管理 (role)                   # RBAC 角色定义
│   ├── 菜单管理 (menu)                   # 权限点配置
│   ├── 字典管理 (dict)                   # 数据字典（活动类型/订单状态等）
│   └── 社交登录 (social)                 # 微信小程序 OAuth 登录（开箱即用）
│
├── yudao-module-club/                   # 【自建】门店模块
│   ├── controller/admin/
│   │   └── ClubController.java          # 经营者管理接口
│   ├── controller/app/
│   │   └── AppClubController.java       # 玩家端公开接口
│   ├── service/
│   │   └── ClubService.java
│   ├── dal/mysql/
│   │   └── ClubMapper.java
│   └── dal/dataobject/
│       └── ClubDO.java
│
├── yudao-module-auth/                   # 【自建】认证扩展模块
│   ├── controller/
│   │   └── AuthController.java          # 小程序登录/管理后台登录
│   ├── service/
│   │   └── AuthService.java             # 调用脚手架内置社交登录+短信模块
│   └── vo/
│       ├── LoginReqVO.java
│       └── LoginRespVO.java             # 含 Sa-Token 签发的 token
│
├── yudao-module-event/                  # 【自建】活动/赛事模块
│   ├── controller/admin/
│   │   └── EventController.java         # 经营者：创建/编辑/管理活动
│   ├── controller/app/
│   │   └── AppEventController.java      # 玩家端：浏览/报名
│   ├── service/
│   │   ├── EventService.java
│   │   └── RegistrationService.java     # 报名逻辑
│   ├── dal/mysql/
│   │   ├── EventMapper.java
│   │   └── RegistrationMapper.java
│   └── dal/dataobject/
│       ├── EventDO.java
│       └── RegistrationDO.java
│
├── yudao-module-product/                # 【自建】商品模块
│   ├── controller/admin/                # 经营者：商品CRUD/分类管理
│   ├── controller/app/                  # 玩家端：浏览商品
│   ├── service/
│   ├── dal/mysql/
│   └── dal/dataobject/
│
├── yudao-module-order/                  # 【自建】订单模块
│   ├── controller/admin/                # 经营者：订单确认/完成/取消
│   ├── controller/app/                  # 玩家端：提交订单/查看历史
│   ├── service/
│   ├── dal/mysql/
│   └── dal/dataobject/
│
├── yudao-module-player/                 # 【自建】玩家/会员模块
│   ├── controller/admin/                # 经营者：会员列表/详情
│   ├── controller/app/                  # 玩家端：个人信息/成绩
│   ├── service/
│   ├── dal/mysql/
│   └── dal/dataobject/
│
├── yudao-module-knowledge/              # 【自建】知识库模块
│   ├── controller/app/                  # 公开：文章列表/详情
│   ├── service/
│   ├── dal/mysql/
│   └── dal/dataobject/
│
├── yudao-module-compliance/             # 【自建】合规中心模块
│   ├── controller/admin/                # 经营者：合规文档/清单
│   ├── service/
│   ├── dal/mysql/
│   └── dal/dataobject/
│
├── yudao-module-leaderboard/            # 【自建】排行榜模块
│   ├── controller/admin/                # 经营者：排名录入
│   ├── controller/app/                  # 玩家端：查看排行榜
│   ├── service/
│   │   └── LeaderboardService.java      # Redis Sorted Set 排行计算
│   └── dal/mysql/
│
└── yudao-module-notification/           # 【自建】通知模块
    ├── service/
    │   ├── WechatTemplateService.java   # 微信模板消息推送
    │   └── WebSocketService.java        # 实时订单通知
    └── listener/
        └── OrderEventListener.java      # 监听订单事件触发推送
```

> **说明**：文件上传（OSS）、短信验证码、社交登录（微信 OAuth）等功能由 `yudao-framework` 提供，无需在业务模块中重复实现。

### 4.4 核心设计模式

| 模式 | 应用场景 | 说明 |
|------|---------|------|
| **统一响应** | 所有 API 返回 `CommonResult<T>` | `{code: 0, msg: "success", data: {...}}`（芋道框架内置） |
| **全局异常处理** | `@RestControllerAdvice` | 统一捕获 `BusinessException`，返回对应错误码 |
| **策略模式** | 积分计算规则 | 不同活动类型可使用不同积分策略 |
| **模板方法** | 微信模板消息推送 | 活动提醒、成绩通知、新订单提醒等共用推送框架 |

---

## 5. API 设计规范

### 5.1 统一响应格式

采用芋道框架内置的 `CommonResult<T>` 统一响应封装：

```json
{
  "code": 0,
  "msg": "success",
  "data": {}
}
```

> **约定**：`code = 0` 表示成功，非 0 表示错误。与 HTTP 状态码解耦，便于前端统一判断。

#### 错误码设计

芋道框架使用 7 位数字错误码，按模块前缀划分：

| 模块 | 前缀 | 示例 |
|------|------|------|
| 全局通用 | `000` | 0000001: 请求参数不正确 |
| 认证模块 | `100` | 1000001: Token 已过期，1000002: 未登录 |
| 门店模块 | `101` | 1010001: 门店不存在，1010002: 非门店经营者 |
| 活动模块 | `102` | 1020001: 活动不存在，1020002: 活动名额已满 |
| 商品模块 | `103` | 1030001: 商品不存在 |
| 订单模块 | `104` | 1040001: 订单不存在，1040002: 订单状态不可变更 |
| 会员模块 | `105` | 1050001: 用户不存在 |
| 知识库模块 | `106` | 1060001: 文章不存在 |

错误码在各模块中以枚举类 `ErrorCodeConstants` 定义，示例：

```java
public interface ErrorCodeConstants {
    ErrorCode CLUB_NOT_EXISTS = new ErrorCode(1010001, "门店不存在");
    ErrorCode CLUB_NOT_OWNER = new ErrorCode(1010002, "您不是该门店的经营者");
}
```

### 5.2 RESTful 风格约定

| 操作 | HTTP 方法 | URL 示例 |
|------|----------|----------|
| 查询列表 | GET | `GET /api/v1/clubs/{clubId}/events?status=OPEN&page=1` |
| 查询详情 | GET | `GET /api/v1/events/{eventId}` |
| 创建 | POST | `POST /api/v1/clubs/{clubId}/events` |
| 更新 | PUT | `PUT /api/v1/events/{eventId}` |
| 部分更新 | PATCH | `PATCH /api/v1/events/{eventId}/status` |
| 删除 | DELETE | `DELETE /api/v1/products/{productId}` |

### 5.3 核心 API 列表

#### 认证模块

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/auth/wechat-login` | 小程序微信登录（code → Sa-Token） |
| POST | `/api/v1/auth/send-sms` | 发送管理后台登录验证码 |
| POST | `/api/v1/auth/phone-login` | 管理后台手机号+验证码登录 |
| POST | `/api/v1/auth/refresh` | 刷新 Token |

#### 门店模块

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/clubs/{clubId}` | 门店详情（公开） |
| PUT | `/api/v1/clubs/{clubId}` | 编辑门店信息（经营者） |
| GET | `/api/v1/clubs/{clubId}/settings` | 获取门店设置（主题/模块开关） |
| PUT | `/api/v1/clubs/{clubId}/settings` | 更新门店设置 |

#### 活动模块

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/clubs/{clubId}/events` | 活动列表（公开） |
| POST | `/api/v1/clubs/{clubId}/events` | 创建活动（经营者） |
| GET | `/api/v1/events/{eventId}` | 活动详情 |
| PUT | `/api/v1/events/{eventId}` | 编辑活动 |
| POST | `/api/v1/events/{eventId}/register` | 玩家报名 |
| DELETE | `/api/v1/events/{eventId}/register` | 取消报名 |
| PUT | `/api/v1/events/{eventId}/results` | 录入排名（经营者） |

#### 商品与订单模块

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/clubs/{clubId}/products` | 商品列表（公开，按分类展示） |
| POST | `/api/v1/clubs/{clubId}/products` | 添加商品（经营者） |
| PUT | `/api/v1/products/{productId}` | 编辑商品 |
| DELETE | `/api/v1/products/{productId}` | 删除商品 |
| GET | `/api/v1/clubs/{clubId}/product-categories` | 分类列表 |
| POST | `/api/v1/clubs/{clubId}/orders` | 玩家提交订单 |
| GET | `/api/v1/clubs/{clubId}/orders` | 订单列表（经营者） |
| GET | `/api/v1/orders/{orderId}` | 订单详情 |
| PATCH | `/api/v1/orders/{orderId}/status` | 更新订单状态（确认/完成/取消） |
| GET | `/api/v1/players/{playerId}/orders` | 玩家历史订单 |

#### 会员与排行榜

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/clubs/{clubId}/members` | 会员列表（经营者） |
| GET | `/api/v1/clubs/{clubId}/leaderboard` | 排行榜（公开） |

#### 知识库与合规

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/knowledge/articles` | 知识库文章列表（公开） |
| GET | `/api/v1/knowledge/articles/{articleId}` | 文章详情 |
| GET | `/api/v1/compliance/docs` | 合规文档列表 |
| GET | `/api/v1/compliance/docs/{docId}` | 合规文档详情 |

#### 文件上传（芋道内置 infra 模块）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/infra/file/upload` | 上传文件（返回 URL，支持图片/文件，芋道内置） |

### 5.4 分页规范

请求参数（芋道框架约定）：

```
?pageNo=1&pageSize=20
```

响应格式：

```json
{
  "code": 0,
  "msg": "success",
  "data": {
    "list": [],
    "total": 100
  }
}
```

> **说明**：芋道框架内置的 `PageResult<T>` 包含 `list`（数据列表）和 `total`（总条数），前端根据 `pageNo`、`pageSize`、`total` 自行计算总页数。

---

## 6. 数据库设计

### 6.1 核心表结构

#### 门店表 (club)

```sql
CREATE TABLE club (
    id          BIGINT PRIMARY KEY AUTO_INCREMENT,
    name        VARCHAR(50)   NOT NULL COMMENT '门店名称',
    logo_url    VARCHAR(500)  COMMENT 'Logo图片URL',
    address     VARCHAR(200)  NOT NULL COMMENT '地址',
    phone       VARCHAR(20)   NOT NULL COMMENT '联系电话',
    open_hours  VARCHAR(100)  COMMENT '营业时间',
    photos      JSON          COMMENT '门店照片URL数组 ["url1","url2"]',
    intro_text  VARCHAR(500)  COMMENT '门店介绍',
    theme_color VARCHAR(20)   DEFAULT '#667eea' COMMENT '主题主色（兼容旧字段）',
    theme_config JSON         COMMENT '完整主题配置 {"primaryColor":"#667eea","secondaryColor":"#764ba2","bgMode":"dark","cardStyle":"glass","fontStyle":"modern","navStyle":"custom"}',
    theme_template VARCHAR(30) DEFAULT 'night_purple' COMMENT '主题模板标识（预设模板名）',
    module_flags JSON         COMMENT '功能模块开关 {"leaderboard":true,"knowledge":true,"menu":true}',
    owner_id    BIGINT        COMMENT '经营者用户ID',
    status      TINYINT       DEFAULT 1 COMMENT '1=正常 0=下线',
    created_at  DATETIME      DEFAULT CURRENT_TIMESTAMP,
    updated_at  DATETIME      DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_owner (owner_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='门店表';
```

#### 用户表 (user)

统一管理小程序玩家和管理后台经营者：

```sql
CREATE TABLE user (
    id          BIGINT PRIMARY KEY AUTO_INCREMENT,
    openid      VARCHAR(64)   COMMENT '微信OpenID（小程序用户）',
    unionid     VARCHAR(64)   COMMENT '微信UnionID',
    phone       VARCHAR(20)   COMMENT '手机号',
    nickname    VARCHAR(50)   COMMENT '昵称',
    avatar_url  VARCHAR(500)  COMMENT '头像URL',
    role        TINYINT       DEFAULT 0 COMMENT '0=玩家 1=经营者 2=管理员',
    total_points INT          DEFAULT 0 COMMENT '累计积分',
    created_at  DATETIME      DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_openid (openid),
    UNIQUE KEY uk_phone (phone)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

#### 活动表 (event)

```sql
CREATE TABLE event (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    club_id         BIGINT        NOT NULL COMMENT '所属门店',
    name            VARCHAR(100)  NOT NULL COMMENT '活动名称',
    event_type      VARCHAR(20)   DEFAULT 'SOCIAL' COMMENT 'SOCIAL=社交赛 TOURNAMENT=锦标赛',
    start_time      DATETIME      NOT NULL COMMENT '开始时间',
    entry_fee       DECIMAL(10,2) COMMENT '报名费（元）',
    max_players     INT           DEFAULT 20 COMMENT '人数上限',
    blind_template  VARCHAR(50)   COMMENT '盲注模板标识',
    prize_desc      VARCHAR(200)  COMMENT '奖品描述',
    status          VARCHAR(20)   DEFAULT 'DRAFT' COMMENT 'DRAFT=草稿 OPEN=招募中 ONGOING=进行中 FINISHED=已结束 CANCELLED=已取消',
    created_at      DATETIME      DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME      DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_club_status (club_id, status),
    INDEX idx_start_time (start_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='活动表';
```

#### 报名记录表 (registration)

```sql
CREATE TABLE registration (
    id          BIGINT PRIMARY KEY AUTO_INCREMENT,
    event_id    BIGINT    NOT NULL,
    user_id     BIGINT    NOT NULL,
    name        VARCHAR(20) COMMENT '报名姓名',
    phone       VARCHAR(20) COMMENT '报名手机号',
    status      VARCHAR(20) DEFAULT 'REGISTERED' COMMENT 'REGISTERED=已报名 CANCELLED=已取消',
    created_at  DATETIME  DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_event_user (event_id, user_id),
    INDEX idx_event (event_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='报名记录表';
```

#### 成绩表 (result)

```sql
CREATE TABLE result (
    id            BIGINT PRIMARY KEY AUTO_INCREMENT,
    event_id      BIGINT NOT NULL,
    user_id       BIGINT NOT NULL,
    rank          INT    NOT NULL COMMENT '名次',
    points_earned INT    DEFAULT 0 COMMENT '获得积分',
    created_at    DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_event_user (event_id, user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='成绩表';
```

#### 门店经营模式配置表 (club_business_config)

```sql
CREATE TABLE club_business_config (
    id                      BIGINT PRIMARY KEY AUTO_INCREMENT,
    club_id                 BIGINT       NOT NULL COMMENT '所属门店',
    template_name           VARCHAR(30)  DEFAULT 'social_light' COMMENT '经营模式模板标识',

    -- 积分获取规则
    consume_points_enabled  TINYINT      DEFAULT 1 COMMENT '消费送积分开关',
    consume_points_ratio    INT          DEFAULT 100 COMMENT '消费1元=X积分',
    consume_points_threshold DECIMAL(10,2) DEFAULT 0 COMMENT '消费满X元才送（0=无门槛）',
    consume_points_fixed    INT          DEFAULT 0 COMMENT '满额固定赠送积分（0=按比率）',
    event_points_enabled    TINYINT      DEFAULT 1 COMMENT '赛事排名送积分开关',
    event_points_template   VARCHAR(20)  DEFAULT 'standard' COMMENT '排名积分模板（standard/average/elimination/custom）',
    event_points_custom     JSON         COMMENT '自定义排名积分 [{"rank":1,"points":500},...]',
    first_visit_bonus       INT          DEFAULT 0 COMMENT '首次到店赠送积分',
    daily_checkin_points    INT          DEFAULT 0 COMMENT '每日签到积分（0=不开启）',

    -- 储值/充值规则
    recharge_enabled        TINYINT      DEFAULT 0 COMMENT '储值功能开关',
    recharge_mode           VARCHAR(20)  DEFAULT 'FULL' COMMENT 'FULL=全额到账 / DISCOUNT=折扣到账 / BONUS=充送',
    recharge_discount_rate  INT          DEFAULT 100 COMMENT '折扣到账比率（70=充100到账70）',
    recharge_bonus_rules    JSON         COMMENT '充送阶梯 [{"amount":500,"bonus":100,"label":"充500送100"},...]',

    -- 积分用途规则
    points_usage            JSON         DEFAULT '["leaderboard"]' COMMENT '积分用途 leaderboard/exchange/deduct/entry',
    exchange_rate           INT          DEFAULT 1000 COMMENT '兑换：X积分=1元商品价值',
    deduct_rate             INT          DEFAULT 0 COMMENT '抵扣：X积分=抵扣1元（0=不开启）',
    entry_points_cost       INT          DEFAULT 0 COMMENT '兑换入场券所需积分（0=不开启）',

    created_at              DATETIME     DEFAULT CURRENT_TIMESTAMP,
    updated_at              DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_club (club_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='门店经营模式配置表';
```

#### 储值记录表 (recharge_record)

```sql
CREATE TABLE recharge_record (
    id          BIGINT PRIMARY KEY AUTO_INCREMENT,
    club_id     BIGINT        NOT NULL,
    user_id     BIGINT        NOT NULL,
    amount      DECIMAL(10,2) NOT NULL COMMENT '充值金额',
    actual      DECIMAL(10,2) NOT NULL COMMENT '实际到账金额',
    bonus       DECIMAL(10,2) DEFAULT 0 COMMENT '赠送金额',
    points_awarded INT        DEFAULT 0 COMMENT '充值赠送积分',
    created_at  DATETIME      DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_club_user (club_id, user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='储值记录表';
```

#### 积分流水表 (points_log)

```sql
CREATE TABLE points_log (
    id          BIGINT PRIMARY KEY AUTO_INCREMENT,
    club_id     BIGINT       NOT NULL,
    user_id     BIGINT       NOT NULL,
    points      INT          NOT NULL COMMENT '积分变动（正=获得，负=消耗）',
    type        VARCHAR(20)  NOT NULL COMMENT 'CONSUME=消费送 / EVENT=赛事 / RECHARGE=充值送 / EXCHANGE=兑换 / DEDUCT=抵扣 / CHECKIN=签到 / BONUS=首次赠送',
    ref_id      BIGINT       COMMENT '关联ID（订单ID/活动ID/充值记录ID）',
    remark      VARCHAR(100) COMMENT '备注说明',
    balance     INT          NOT NULL COMMENT '变动后余额',
    created_at  DATETIME     DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_club_user (club_id, user_id),
    INDEX idx_type (type)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='积分流水表';
```

#### 商品表 (product)

```sql
CREATE TABLE product (
    id          BIGINT PRIMARY KEY AUTO_INCREMENT,
    club_id     BIGINT       NOT NULL,
    category_id BIGINT       COMMENT '分类ID',
    name        VARCHAR(50)  NOT NULL COMMENT '商品名称',
    image_url   VARCHAR(500) COMMENT '商品图片',
    price       DECIMAL(10,2) NOT NULL COMMENT '价格',
    description VARCHAR(200) COMMENT '简介',
    sort_order  INT          DEFAULT 0 COMMENT '排序',
    is_sold_out TINYINT      DEFAULT 0 COMMENT '0=在售 1=售罄',
    created_at  DATETIME     DEFAULT CURRENT_TIMESTAMP,
    updated_at  DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_club_category (club_id, category_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='商品表';
```

#### 商品分类表 (product_category)

```sql
CREATE TABLE product_category (
    id         BIGINT PRIMARY KEY AUTO_INCREMENT,
    club_id    BIGINT      NOT NULL,
    name       VARCHAR(30) NOT NULL COMMENT '分类名',
    sort_order INT         DEFAULT 0,
    INDEX idx_club (club_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='商品分类表';
```

#### 订单表 (order)

```sql
CREATE TABLE `order` (
    id           BIGINT PRIMARY KEY AUTO_INCREMENT,
    order_no     VARCHAR(32)  NOT NULL COMMENT '订单号（展示用）',
    club_id      BIGINT       NOT NULL,
    user_id      BIGINT       NOT NULL COMMENT '下单用户',
    table_no     VARCHAR(10)  COMMENT '桌号',
    total_amount DECIMAL(10,2) NOT NULL COMMENT '订单金额',
    status       VARCHAR(20)  DEFAULT 'PENDING' COMMENT 'PENDING=待确认 CONFIRMED=已确认 COMPLETED=已完成 CANCELLED=已取消',
    remark       VARCHAR(100) COMMENT '备注',
    created_at   DATETIME     DEFAULT CURRENT_TIMESTAMP,
    updated_at   DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_order_no (order_no),
    INDEX idx_club_status (club_id, status),
    INDEX idx_user (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='订单表';
```

#### 订单明细表 (order_item)

```sql
CREATE TABLE order_item (
    id           BIGINT PRIMARY KEY AUTO_INCREMENT,
    order_id     BIGINT        NOT NULL,
    product_id   BIGINT        NOT NULL,
    product_name VARCHAR(50)   NOT NULL COMMENT '商品名称（快照）',
    price        DECIMAL(10,2) NOT NULL COMMENT '单价（快照）',
    quantity     INT           NOT NULL DEFAULT 1,
    INDEX idx_order (order_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='订单明细表';
```

#### 知识库文章表 (knowledge_article)

```sql
CREATE TABLE knowledge_article (
    id           BIGINT PRIMARY KEY AUTO_INCREMENT,
    title        VARCHAR(100) NOT NULL COMMENT '标题',
    category     VARCHAR(30)  NOT NULL COMMENT '分类: RULES/EVENT/COCKTAIL/COMPLIANCE/BIZ',
    cover_url    VARCHAR(500) COMMENT '封面图',
    summary      VARCHAR(200) COMMENT '摘要',
    content      TEXT         NOT NULL COMMENT '正文（Markdown）',
    read_count   INT          DEFAULT 0 COMMENT '阅读量',
    is_published TINYINT      DEFAULT 1 COMMENT '0=草稿 1=已发布',
    created_at   DATETIME     DEFAULT CURRENT_TIMESTAMP,
    updated_at   DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_category (category)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='知识库文章表';
```

#### 合规文档表 (compliance_doc)

```sql
CREATE TABLE compliance_doc (
    id         BIGINT PRIMARY KEY AUTO_INCREMENT,
    title      VARCHAR(100) NOT NULL,
    category   VARCHAR(30)  NOT NULL COMMENT 'REDLINE/POSITIONING/CHECKLIST/TEMPLATE',
    content    TEXT         NOT NULL,
    sort_order INT          DEFAULT 0,
    INDEX idx_category (category)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='合规文档表';
```

### 6.2 ER 关系图

```
  ┌──────────┐       ┌──────────┐       ┌──────────┐
  │   club   │1────*│  event   │*────*│   user   │
  └────┬─────┘       └────┬─────┘       └────┬─────┘
       │                  │                  │
       │1                 │1                 │1
       │                  │                  │
  ┌────┴─────┐     ┌──────┴──────┐    ┌─────┴──────┐
  │ product  │     │registration │    │   result   │
  └──────────┘     └─────────────┘    └────────────┘
       │
       │1
       │
  ┌────┴──────┐     ┌───────────┐
  │  order    │1────│order_item │
  └───────────┘     └───────────┘
```

### 6.3 Redis 缓存策略

| 缓存 Key | 内容 | 过期时间 | 说明 |
|----------|------|---------|------|
| `satoken:login:token:{token}` | 用户登录态 | 7 天（active-timeout 2h） | Sa-Token 自动管理，无需手动维护 |
| `satoken:login:session:{userId}` | 用户 Session 数据 | 随 Token 过期 | 存储 role/clubId 等扩展信息 |
| `sms:code:{phone}` | 短信验证码 | 5 分钟 | 登录用（芋道内置短信模块） |
| `club:info:{clubId}` | 门店信息 | 30 分钟 | 高频读取，门店信息变更时主动失效 |
| `event:list:{clubId}` | 活动列表 | 5 分钟 | 首页高频访问 |
| `leaderboard:{clubId}` | 排行榜数据（Sorted Set） | 10 分钟 | Redis ZREVRANGE 实现高效排名查询 |
| `order:new:{clubId}` | 是否有新订单（标记位） | 实时更新 | 经营者端红点提醒，确认后清除 |

---

## 7. 认证与安全

### 7.1 小程序登录流程

```
┌──────────┐          ┌──────────┐          ┌──────────┐
│  小程序   │          │  后端     │          │ 微信服务  │
└────┬─────┘          └────┬─────┘          └────┬─────┘
     │                     │                     │
     │ 1. wx.login()      │                     │
     ├──────────────────────────────────────────>│
     │                     │                     │
     │ 2. 返回 code       │                     │
     │<──────────────────────────────────────────┤
     │                     │                     │
     │ 3. POST /auth/wechat-login {code}         │
     ├────────────────────>│                     │
     │                     │                     │
     │                     │ 4. code2Session     │
     │                     ├────────────────────>│
     │                     │                     │
     │                     │ 5. 返回 openid      │
     │                     │<────────────────────┤
     │                     │                     │
     │                     │ 6. 查询/创建用户     │
     │                     │ 7. Sa-Token 登录签发 Token │
     │                     │                     │
     │ 8. 返回 {token, user}                     │
     │<────────────────────┤                     │
     │                     │                     │
     │ 9. 存储 Token       │                     │
     │ 10. 后续请求携带     │                     │
     │    Authorization头   │                     │
```

### 7.2 管理后台登录流程

```
经营者手机 → 输入手机号 → 点击"获取验证码"
  → 后端发送短信验证码 → Redis 存储(code, 5min)
  → 用户输入验证码 → POST /auth/phone-login
  → 后端校验 → 查询/注册用户（role=operator）→ Sa-Token 登录签发 Token
  → 前端存储 Token → 后续请求携带 Authorization: Bearer {token}
```

### 7.3 Token 设计（Sa-Token 方案）

Sa-Token 内置 Token 生命周期管理，替代手动 JWT 签发/验证逻辑：

| 配置项 | 值 | 说明 |
|--------|-----|------|
| `sa-token.token-name` | `Authorization` | 请求头名称 |
| `sa-token.timeout` | `604800` (7天) | Token 有效期 |
| `sa-token.active-timeout` | `7200` (2小时) | 无操作自动过期（临时有效期） |
| `sa-token.is-concurrent` | `true` | 允许多端同时登录（小程序+管理后台） |
| `sa-token.token-style` | `uuid` | Token 风格，安全且无信息泄露 |

**登录后 Token 携带方式**：前端将 Token 放入 `Authorization: Bearer {token}` 请求头。

**多端登录隔离**：Sa-Token 通过 `device` 参数区分小程序端和管理后台端，两端独立管理 Token 生命周期，互不影响。

**Session 扩展数据**：登录后在 Sa-Token Session 中存储用户附加信息（避免每次查库）：

```java
// 登录时写入 Session
StpUtil.getSession().set("userId", user.getId());
StpUtil.getSession().set("role", user.getRole());     // player | operator | admin
StpUtil.getSession().set("clubId", user.getClubId()); // 经营者关联的门店ID
```

### 7.4 安全措施

| 措施 | 实现方式 |
|------|---------|
| HTTPS | Nginx 配置 SSL 证书，全站加密 |
| SQL 注入防护 | MyBatis-Plus 参数化查询，`#{}` 占位符 |
| XSS 防护 | 后端输出编码，前端 Vant 组件自带转义 |
| CSRF | 无状态 Token 认证，不依赖 Cookie，天然免疫 CSRF |
| 接口鉴权 | Sa-Token 注解式鉴权：`@SaCheckLogin`（登录校验）+ `@SaCheckRole("operator")`（角色校验）+ `@SaCheckPermission("event:create")`（细粒度权限） |
| 敏感数据脱敏 | 手机号中间4位替换为 `****`，日志中不打印完整手机号 |
| 验证码防刷 | 同一手机号 60 秒内只能发送一次，每天最多 5 次 |
| 文件上传安全 | 限制文件类型（仅图片）、大小（≤5MB）、校验文件魔数 |
| 接口限流 | Sa-Token 内置 `@SaCheckSafe` 二次认证 + 自定义限流注解防止恶意请求 |

---

## 8. 部署架构

### 8.1 MVP 部署方案

推荐使用 **微信云托管**（降低运维成本 + 免备案免 HTTPS 配置），备选 **阿里云 ECS**。

```
┌──────────────────────────────────────────────────────────────┐
│                    微信云托管 / 阿里云 ECS                     │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │                      Nginx                            │   │
│  │                                                       │   │
│  │  /api/*     ──→  Spring Boot :8080  (后端 API)        │   │
│  │  /admin/*   ──→  /opt/poker-go-system/  (统一后台 SPA) │   │
│  │  /uploads/* ──→  /opt/static/   (上传的图片文件)      │   │
│  │                                                       │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌────────────────┐  ┌────────────┐  ┌──────────────────┐  │
│  │  Spring Boot   │  │   MySQL    │  │     Redis        │  │
│  │  Jar  :8080    │  │   :3306    │  │     :6379        │  │
│  │                │  │            │  │                  │  │
│  │  业务逻辑层     │  │  持久化存储  │  │  缓存 / Session  │  │
│  └────────────────┘  └────────────┘  └──────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │             腾讯云 COS (图片/文件存储)                  │   │
│  │  门店照片 / 商品图片 / 知识库配图                       │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  微信小程序 (部署在微信服务器，非本机)                   │   │
│  │  通过 HTTPS 调用 API，微信审核后发布                    │   │
│  └──────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────┘
```

### 8.2 Docker Compose（本地开发/测试）

```yaml
version: '3.8'
services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: pokergo123
      MYSQL_DATABASE: pokergo
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  server:
    build: ./poker-go-server
    ports:
      - "8080:8080"
    depends_on:
      - mysql
      - redis
    environment:
      SPRING_PROFILES_ACTIVE: dev

volumes:
  mysql_data:
```

### 8.3 环境变量

```yaml
# application-prod.yml 关键配置（基于 ruoyi-vue-pro 配置结构）
spring:
  datasource:
    dynamic:
      primary: master
      datasource:
        master:
          url: ${DB_URL:jdbc:mysql://localhost:3306/pokergo?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8mb4}
          username: ${DB_USERNAME}
          password: ${DB_PASSWORD}
  redis:
    host: ${REDIS_HOST:localhost}
    port: ${REDIS_PORT:6379}
    password: ${REDIS_PASSWORD:}

# Sa-Token 认证配置
sa-token:
  token-name: Authorization
  timeout: 604800            # Token 有效期 7 天（秒）
  active-timeout: 7200       # 无操作过期 2 小时（秒）
  is-concurrent: true        # 允许多端同时在线
  token-style: uuid

# 芋道业务配置（统一在 yudao 命名空间下）
yudao:
  # 微信小程序配置（芋道内置社交登录模块）
  social:
    wechat-mini-app:
      app-id: ${WECHAT_APP_ID}
      app-secret: ${WECHAT_APP_SECRET}
  # 短信服务（芋道内置短信模块）
  sms:
    channels:
      - code: ALIYUN         # 或 TENCENT
        api-key: ${SMS_API_KEY}
        api-secret: ${SMS_API_SECRET}

# 对象存储（芋道内置 OSS 模块，通过管理后台可视化配置）
# 支持腾讯云 COS / 阿里云 OSS / MinIO，配置存储在数据库 infra_file_config 表中
```

---

## 9. 项目结构

### 9.1 代码仓库

推荐 **单体仓库（Monorepo）** 两个前端子项目 + 一个后端项目，后端基于 ruoyi-vue-pro 脚手架。统一管理后台基于 `yudao-ui-admin-vben`（Vben 5.x Monorepo 架构）：

```
poker/
├── poker-go-miniapp/            # 微信小程序（玩家端，uni-app Vue 3）
│   ├── pages/                   # 页面（.vue SFC）
│   ├── components/              # 通用组件
│   ├── api/                     # 接口层
│   ├── stores/                  # Pinia 状态管理
│   ├── manifest.json            # uni-app 配置（AppID/编译目标）
│   ├── pages.json               # 路由配置
│   └── env.js                   # 环境/白标配置
│
├── poker-go-system/             # 统一管理后台（基于 yudao-ui-admin-vben Monorepo）
│   ├── apps/
│   │   └── web-antd/            # Ant Design Vue 应用（主入口）
│   │       └── src/
│   │           ├── views/
│   │           │   ├── system/  # 【脚手架内置】系统管理（租户/用户/角色/字典）
│   │           │   ├── poker/   # 【自建】PokerGo 业务页面（工作台/活动/订单/商品...）
│   │           │   └── infra/   # 【脚手架内置】基础设施（文件/日志/任务）
│   │           ├── router/
│   │           ├── store/       # 【脚手架内置 + 自建扩展】Pinia 状态管理
│   │           └── api/         # 【脚手架内置 + 自建扩展】API 封装
│   ├── packages/                # Vben 共享包（不改动）
│   ├── internal/                # Vben 构建配置（不改动）
│   ├── package.json             # monorepo 根配置（pnpm workspace）
│   ├── turbo.json               # Turborepo 任务编排
│   └── pnpm-workspace.yaml
│
├── poker-go-server/             # 后端（基于 ruoyi-vue-pro 脚手架）
│   ├── poker-server-framework/  # 【脚手架】核心框架 Starter
│   │   ├── poker-server-common/
│   │   ├── poker-server-spring-boot-starter-biz-tenant/  # 【关键】多租户 Starter
│   │   ├── poker-server-spring-boot-starter-security/    # Sa-Token 认证
│   │   └── ...
│   ├── poker-server-module-system/     # 【脚手架】系统管理
│   ├── poker-server-module-infra/      # 【脚手架】基础设施
│   ├── poker-server-module-club/       # 【自建】门店模块
│   ├── poker-server-module-event/      # 【自建】活动/赛事模块
│   ├── poker-server-module-product/    # 【自建】商品模块
│   ├── poker-server-module-order/      # 【自建】订单模块
│   ├── poker-server-module-player/     # 【自建】玩家/会员模块
│   ├── poker-server-module-knowledge/  # 【自建】知识库模块
│   ├── poker-server-module-compliance/ # 【自建】合规中心模块
│   ├── poker-server-module-leaderboard/# 【自建】排行榜模块
│   ├── poker-server-module-notification/# 【自建】通知推送模块
│   ├── poker-server-server/            # 启动模块（聚合所有模块）
│   ├── pom.xml                  # 父 POM
│   ├── Dockerfile
│   └── sql/                     # 数据库初始化脚本
│       ├── system.sql           # 脚手架系统表（含 system_tenant / system_tenant_package）
│       └── business.sql         # 业务表（club/event/product/order... 含 tenant_id 字段）
│
├── docs/                        # 产品/技术文档
│   ├── 产品调研文档.md
│   ├── MVP产品需求文档.md
│   └── 技术架构设计.md
│
└── docker-compose.yml           # 本地开发环境（MySQL + Redis）
```

### 9.2 开发环境要求

| 工具 | 版本 | 说明 |
|------|------|------|
| JDK | 21+ | 后端运行环境（LTS，虚拟线程 GA） |
| Maven | 3.8+ | 后端构建 |
| Node.js | ^22.18.0 | 前端构建（Vben 5 要求 v22+） |
| pnpm | >=11.0.0 | 前端包管理（强制） |
| MySQL | 8.0+ | 数据库 |
| Redis | 7.x | 缓存 |
| HBuilderX | 4.x+ | uni-app 开发/编译/运行 |
| 微信开发者工具 | 最新稳定版 | 小程序预览调试（配合 HBuilderX 使用） |
| Docker | 24+ | 容器化部署（可选） |

### 9.3 本地启动步骤

```bash
# 1. 启动中间件（MySQL 8.0 + Redis 7）
docker-compose up -d mysql redis

# 2. 初始化数据库
#    导入 poker-go-server/sql/system.sql（脚手架系统表）
#    导入 poker-go-server/sql/business.sql（业务表）

# 3. 启动后端（基于 ruoyi-vue-pro 脚手架）
cd poker-go-server
mvn clean compile -pl poker-server-server -am    # 首次编译所有模块
mvn spring-boot:run -pl poker-server-server -Dspring-boot.run.profiles=dev

# 4. 启动统一管理后台（基于 yudao-ui-admin-vben Monorepo）
cd poker-go-system
pnpm install                             # 安装全部子包依赖
pnpm dev:antd                            # 启动 web-antd 应用
# 访问 http://localhost:5173

# 5. 启动小程序（HBuilderX 打开 poker-go-miniapp 目录）
#    配置 manifest.json 中的微信小程序 AppID
#    配置 env.js 中的后端 API 地址
#    点击"运行到微信开发者工具"即可预览调试

# 6. 构建统一管理后台
pnpm build:antd                          # 构建 web-antd 到 dist/
# 部署 dist/ 到 Nginx /admin/ 目录
```
```

## 10. 后端脚手架选型

### 10.1 选型背景

从头搭建 Spring Boot 项目需要处理大量基础工作：认证鉴权、RBAC 权限、全局异常处理、API 文档、代码生成器、文件上传、短信服务等。这些虽是"标配"功能，但全部从零开发会消耗 MVP 宝贵的 6-8 周周期。因此，选择一个成熟的后端脚手架作为基座，在其上开发业务代码，是最务实的策略。

**关键约束**：我们的管理后台使用 Vue 3 + Vant 4（移动端优先），不依赖任何 PC 端 Admin 模板。因此只使用脚手架的后端部分，其自带的前端直接弃用。

### 10.2 候选框架对比

| 维度 | 芋道 ruoyi-vue-pro | RuoYi-Vue-Plus | BallCat | ContiNew-Admin | JeecgBoot |
|------|-------------------|----------------|---------|----------------|-----------|
| **GitHub Stars** | 30k+ | 5k+ | 2k+ | 3k+ | 40k+ |
| **Spring Boot** | 3.4.x ✅ | 3.x ✅ | 3.x ✅ | 3.x ✅ | 3.x ✅ |
| **JDK** | 21 ✅ | 21 ✅ | 17+ ✅ | 17+ ✅ | 17+ ✅ |
| **ORM** | MyBatis-Plus ✅ | MyBatis-Plus ✅ | MyBatis-Plus ✅ | MyBatis-Plus ✅ | MyBatis-Plus ✅ |
| **认证方案** | Sa-Token + JWT | Sa-Token + JWT | Spring Security | Sa-Token + JWT | Spring Security |
| **权限模型** | RBAC（角色-菜单-权限） | RBAC + 多租户 | RBAC | RBAC | RBAC + 数据权限 |
| **代码生成器** | ★★★★★ 最强 | ★★★★ 强 | ★★★ 中等 | ★★★ 中等 | ★★★★★ 强 |
| **API 文档** | Knife4j ✅ | Knife4j ✅ | Swagger | Knife4j ✅ | Knife4j ✅ |
| **文件存储** | 内置 OSS（COS/OSS/S3）| 内置 OSS | 无（需自建） | 内置 OSS | 内置 OSS |
| **短信服务** | 内置（阿里/腾讯云）| 内置 | 无 | 无 | 内置 |
| **微信集成** | 内置小程序登录 ✅ | 内置小程序登录 ✅ | 无 | 无 | 内置 |
| **多租户** | 支持 | 支持 | 不支持 | 最新版支持 | 支持 |
| **模块化** | 按功能拆分为独立 Maven 模块 | 按功能拆分 | 按 Starter 拆分 | 单模块 | 按功能拆分 |
| **前端框架** | Element Plus (PC) | Element Plus (PC) | Ant Design Vue (PC) | Naive UI (PC) | Ant Design Vue (PC) |
| **文档质量** | ★★★★★ 中文视频+文档 | ★★★★ 中文文档 | ★★★ 部分英文 | ★★★ 中文文档 | ★★★ 中文文档 |
| **社区活跃度** | 极高（微信群/知识星球） | 高 | 中 | 成长中 | 高 |
| **许可证** | MIT ✅ | MIT ✅ | Apache 2.0 ✅ | MIT ✅ | Apache 2.0 ✅ |
| **适合 PokerGo** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ |

### 10.3 选型结论

#### 首选：芋道源码 ruoyi-vue-pro（后端部分）

**核心理由**：

1. **最完整的"开箱即用"**：内置微信小程序登录、腾讯云 COS、短信验证码、Knife4j 文档 —— 恰好都是 PokerGo MVP 的刚需，省去大量集成时间
2. **最好的中文文档和社区**：近 30K Star，有完整的视频教程、开发文档、微信群、知识星球，团队遇到问题能快速找到答案
3. **Sa-Token 认证方案比 Spring Security 更简洁**：注解式鉴权（`@SaCheckPermission`），配置量不到 Security 的 1/3，非常适合 MVP 快速开发
4. **代码生成器最强大**：连接数据库 → 选择表 → 一键生成 Controller / Service / ServiceImpl / Mapper / Entity / VO / Excel，生成的代码质量高，几乎不需要手动修改
5. **模块化设计**：可以只保留需要的模块，不需要的功能（如工作流、CRM、支付）直接删除或忽略

**我们的使用方式**：

```
ruoyi-vue-pro 完整项目
│
├── ✅ 保留：后端 Java 模块
│   ├── yudao-framework/     # 核心框架（认证、权限、ORM、文档、OSS）
│   ├── yudao-module-system/ # 系统管理（用户、角色、菜单、字典）
│   └── yudao-module-xxx/    # 在此之上开发我们的业务模块
│
├── ❌ 弃用：前端模块
│   └── yudao-ui-admin-vue3/ # PC 端 Element Plus 管理后台
│
├── ✅ 自建：我们的前端
│   └── poker-go-admin/      # Vue 3 + Vant 4 移动端管理后台
│
└── ✅ 自建：微信小程序
    └── poker-go-miniapp/     # uni-app (Vue 3) 小程序
```

**具体操作**：

1. Clone ruoyi-vue-pro，删除 `yudao-ui-admin-vue3/` 等前端目录
2. 删除不需要的业务模块（如 BPM 工作流、MP 公众号、CRM 等）
3. 在 `yudao-module-system` 基础上新建我们的业务模块：`yudao-module-club/`（门店）、`yudao-module-event/`（活动）、`yudao-module-order/`（订单）等
4. 数据库保留系统表（用户/角色/菜单/字典），新建业务表（club / event / product / order 等）
5. 我们的 Vue 3 + Vant 管理后台和小程序调用同一套 RESTful API

#### 备选：ContiNew-Admin（如果追求极致轻量）

如果团队希望从更轻量的基础开始（不必删除大量用不上的模块），ContiNew-Admin 是不错的备选。它同样基于 Spring Boot 3.x + Sa-Token + MyBatis-Plus，但功能集更精简：只有认证、RBAC、代码生成器、数据脱敏等核心功能，没有工作流、CRM 等重模块。

**适用场景**：团队想从更干净的基础开始，愿意花少量时间自行集成 OSS、短信、微信登录等功能。

#### 为什么不选其他

| 框架 | 不选理由 |
|------|---------|
| **RuoYi-Vue-Plus** | 功能集与 ruoyi-vue-pro 高度重叠，社区和文档不如后者 |
| **BallCat** | Starter 设计优雅，但功能完整度不够，缺少微信登录、OSS 集成等开箱模块 |
| **JeecgBoot** | 太重，强依赖其低代码 IDE 和 PC 端 UI，与我们的 Vue+Vant 技术路线冲突 |

### 10.4 集成后的后端模块规划

基于 ruoyi-vue-pro 脚手架，我们的业务模块遵循芋道的标准目录规范：

```
yudao-module-club/                    # 门店管理（示例完整结构）
├── controller/
│   ├── admin/
│   │   └── ClubController.java       # 管理后台 API（经营者调用）
│   └── app/
│       └── AppClubController.java    # 小程序 API（玩家调用）
├── service/
│   ├── ClubService.java              # 接口
│   └── ClubServiceImpl.java          # 实现
├── dal/
│   ├── mysql/
│   │   └── ClubMapper.java           # MyBatis-Plus Mapper
│   └── dataobject/
│       └── ClubDO.java               # 数据库实体
├── convert/
│   └── ClubConvert.java              # MapStruct 对象转换器
└── vo/                               # 请求/响应 VO
    ├── ClubCreateReqVO.java
    ├── ClubUpdateReqVO.java
    └── ClubRespVO.java

# 其他业务模块结构同上
yudao-module-event/                   # 活动/赛事管理
yudao-module-product/                 # 商品管理
yudao-module-order/                   # 订单管理
yudao-module-player/                  # 玩家/会员
yudao-module-knowledge/               # 知识库
yudao-module-compliance/              # 合规中心
yudao-module-leaderboard/             # 排行榜
yudao-module-notification/            # 通知推送
```

---


>
> **下一步**：  
> 1. Clone ruoyi-vue-pro 并裁剪：删除前端模块和不需要的业务模块（BPM/CRM/MP），保留 framework + system + infra  
> 2. 创建业务模块骨架：`yudao-module-club`、`yudao-module-event`、`yudao-module-order` 等  
> 3. 搭建管理后台项目骨架：Vue 3 + Vite 6 + Vant 4 + UnoCSS + Pinia（暗色主题）  
> 4. 建立微信小程序项目骨架：原生开发 + WeUI + 请求封装  
> 5. 导入数据库脚本（system.sql + business.sql），验证后端启动正常  
> 6. 实现第一个端到端功能：门店创建（开店向导 Step 1-4 → 后端 API → 小程序展示）
